# premier_projet

## Lancer le backend

```bash
cd backend
npm install
npm start
```

Pour utiliser MongoDB, creer un fichier `.env` dans le dossier `backend` a partir de `.env.example`, puis renseigner `MONGODB_URI`.

Le serveur ecoute par defaut sur le port `3000`.
